controller_list:
  - name: fake_arm_group_controller
    type: $(arg execution_type)
    joints:
      - joint_0
      - joint_1
      - joint_2
      - joint_3
      - joint_4
      - joint_5
initial:  # Define initial robot poses.
  - group: arm_group
    pose: all_zero